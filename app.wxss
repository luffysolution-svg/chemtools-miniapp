/* 
 * 全局样式 - 性能优化版
 * 只包含最基础的样式，其他样式按需在页面中引入
 * 优化策略：减少首次加载，提升启动速度
 */

page {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
  background-color: #f2f5fa;
  color: #2d3142;
  font-size: 28rpx;
  line-height: 1.6;
}

view, text, button, input {
  box-sizing: border-box;
}

button {
  background: transparent;
  border: none;
  padding: 0;
  font: inherit;
}

/* 
 * 高级样式按需引入：
 * - animations.wxss: 需要动画的页面引入
 * - responsive.wxss: 需要响应式的页面引入
 * - accessibility.wxss: 需要无障碍的页面引入
 * - dark-mode.wxss: 需要深色模式的页面引入
 */
